# gallery_v2
> 前後端不分離，後端簡易使用到PHP，前端則是Nuxt.js，因此用到Docker起，nginx導向。

## Up & Stop
```
docker-compose up -d
docker-compose down
```
## URL
```
localhost:8080
```

## Check 
```
docker-compose logs -f
```

